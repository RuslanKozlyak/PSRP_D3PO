name: benchmark
device: cpu
total_iterations: 100
rollout_length: 64
n_envs: 1
eval_interval: 5
eval_episodes: 6
log_interval: 1
save_interval: 10
comparison_eval_episodes: 8
comparison_algorithms: [ppo, ppo_lagrangian, d3po]
d3po_eval_preference: auto
experiment_seeds: [7, 17, 29]
