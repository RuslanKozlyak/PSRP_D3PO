name: smoke
device: cpu
total_iterations: 6
rollout_length: 12
n_envs: 1
eval_interval: 2
eval_episodes: 1
log_interval: 1
save_interval: 0
comparison_eval_episodes: 2
comparison_algorithms: [ppo, ppo_lagrangian, d3po]
d3po_eval_preference: auto
experiment_seeds: [7]
